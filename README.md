Install:

git clone http://github.com/rthornton128/calc1
cd github.com/rthornton128/calc1/comp
go install

*go get should work to get the compiler but will not pull in the build
scripts*

Usage:

calc <filename>.calc
